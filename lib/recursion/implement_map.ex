defmodule Tutorials.Recursion.Map do
  # def map(list,_, acc \\ [])
  # def map([],_, acc), do: acc
  # def map([h | t],func,acc) do
  #   map(t,func,[func.(h)|acc])
  # end


  def hello() do
    "hello"
  end
  def world(txt) do
    txt <> "world"
  end
end
